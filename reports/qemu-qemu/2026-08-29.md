# AI policy coverage: qemu/qemu

**Metric:** [Consent Policy Specificity](https://github.com/chaoss/wg-ai-alignment/blob/main/metrics/ai-alignment-community-governed-use/ai-use-consent-policy-specificity.md) · CHAOSS AI Alignment, *Community Governed Use*
**Commit:** `d2e570cc0f97b936902a5b1b86b73c0f5998b475`
**Files scanned:** `docs/devel/code-provenance.rst`

> Scope was limited to the files above, so **not addressed** here means *not addressed in those files*, not that the project says nothing repository-wide.

## Coverage matrix

Every populated cell is footnoted to the line it rests on.

| Domain | Addressed | Lean | Supervision | Scope | Accountability | Proportionality |
|---|---|---|---|:-:|:-:|:-:|
| Code contributions | **yes** | – | `banned`[^fn1] | ✓[^fn2] | ✓[^fn3] | – |
| Notetaker / meeting bots | **no** | – | – | – | – | – |
| Content | **no** | – | – | – | – | – |
| Moderation actions | **no** | – | – | – | – | – |
| Review | **no** | – | – | – | – | – |
| Autonomous / agentic use | **yes** | – | `banned`[^fn4] | ✓[^fn5] | ✓[^fn6] | – |
| Environmental Impact | **no** | – | – | – | – | – |
| Infrastructure strain | **no** | – | – | – | – | – |
| Data use for training | **no** | *restrictive*[^fn7] | – | – | – | – |

**Leans without a rule (1):** Data use for training (*restrictive*)

These domains have no rule, but the policy shows a direction: a stated concern, objection, or openness. Quoted under *Evidence*. Worth knowing if you are deciding whether to contribute: it is where a project is likely to go next.

**Addressed (2):** Code contributions, Autonomous / agentic use
**Partial (0):** none
**In rationale only (1):** Data use for training
**Not mentioned (6):** Notetaker / meeting bots, Content, Moderation actions, Review, Environmental Impact, Infrastructure strain

**Overall posture:** The document, QEMU's code-provenance policy, imposes a firm, specifically-scoped ban on AI-generated or AI-assisted content (including agentic tools) in patch contributions and pins accountability to the submitting contributor via Signed-off-by, but it is silent on every other domain (notetaker bots, general content, moderation, AI-assisted review, environmental impact, infrastructure strain, and training-data use), with copyright/licensing risk of training data cited only as rationale for the contribution ban rather than as a rule in its own right.

<details>
<summary><strong>Legend: what each column and domain means</strong></summary>

**Addressed** rolls up the four attributes for that domain:

| | |
|---|---|
| `yes` | supervision level stated, plus at least one of scope, accountability, proportionality |
| `partial` | the domain is named but only one attribute is stated, or it is covered only by general wording that never names it |
| `no` | checked; the policy does not mention this domain |

**Lean**: a direction without a rule. A project that cites energy and water use as a reason to ban AI in code has not set an environmental *rule*, but it is plainly not neutral either. `restrictive` means it voices concern or caution; `permissive` means it signals openness or declines to restrict; `–` means no indication either way. A lean never changes **Addressed**. It records disposition, not policy.

**Attributes**: the four consent-type attributes from the metric. `✓` means stated, `–` means not stated. `–` is a finding, not a gap in the scan.

| Attribute | What counts |
|---|---|
| Supervision | banned · human-in-the-loop · disclosure required · limited unsupervised · fully unsupervised |
| Scope | a stated limit on volume, size, or kind of use |
| Accountability | who is answerable for the AI-assisted output |
| Proportionality | resource use relative to contributor count or community size |

**Domains**: where AI shows up. A policy addressing one says nothing about the others; that is what this metric measures.

| Domain | Looking for |
|---|---|
| Code contributions | PRs, issues, comments |
| Notetaker / meeting bots | recorded discussion, closed or small-group content |
| Content | documentation, blogs, design assets |
| Moderation actions | AI flagging, hiding, tagging, deleting, or triaging |
| Review | who or what may review using AI |
| Autonomous / agentic use | agents acting without a human in the loop per action |
| Environmental Impact | energy use, water use, hardware/carbon footprint |
| Infrastructure strain | server load, hardware cost/financing |
| Data use for training | platform user data |

A statement counts for a domain only if it names that activity. A blanket line such as “AI-assisted contributions must be disclosed” is evidence about code contributions and nothing else. Those appear under *Blanket statements* below.

</details>

## Evidence

### Code contributions: yes
> Current QEMU project policy is to DECLINE any contributions which are believed to include or derive from AI generated content.

`docs/devel/code-provenance.rst:293`

> the "Signed-off-by" label in a patch submission is a statement that the author takes responsibility for the entire contents of the patch, including any parts that were generated or assisted by AI tools

`docs/devel/code-provenance.rst:346`

### Autonomous / agentic use: yes
> Examples of tools impacted by this policy includes GitHub's CoPilot, OpenAI's ChatGPT, Anthropic's Claude, and Meta's Code Llama, and code/content generation agents which are built on top of such tools.

`docs/devel/code-provenance.rst:329`

### Data use for training: policy rationale
*Policy rationale. Given as a reason given for another rule, not a rule about this domain:* “Where the training material is known, it is common for it to include large volumes of material under restrictive licensing/copyright terms.”

**Checked, no policy text found (6):** Notetaker / meeting bots, Content, Moderation actions, Review, Environmental Impact, Infrastructure strain

## Suggested improvements

What the policy would need to state to close each gap. These describe *missing specificity*, not a recommended position. How permissive or restrictive to be is the community's decision.

- **Notetaker / meeting bots** (no) — no supervision level or accountability holder for meeting/notetaker bots
- **Content** (no) — no rule naming documentation, blogs, or design assets specifically
- **Moderation actions** (no) — no supervision level or accountability holder for AI in moderation
- **Review** (no) — no rule on whether reviewers may use AI to review patches
- **Environmental Impact** (no) — no rule or rationale on energy/water/carbon impact of AI
- **Infrastructure strain** (no) — no rule or rationale on server/infrastructure strain from AI
- **Data use for training** (no) — no rule governing use of platform/user data for training

---

*Generated with `claude-sonnet-5`. 14,248 input / 4,701 output tokens over 2 API calls, ~$0.0755.*

[^fn1]: “Current QEMU project policy is to DECLINE any contributions which are believed to include or derive from AI generated content.” — `docs/devel/code-provenance.rst:293`
[^fn2]: Applies to patches/contributions submitted to the QEMU project; does not apply to other AI uses (API research, static analysis, debugging) whose output is not included in the contribution.
[^fn3]: contributor (patch author)
[^fn4]: “Examples of tools impacted by this policy includes GitHub's CoPilot, OpenAI's ChatGPT, Anthropic's Claude, and Meta's Code Llama, and code/content generation agents which are built on top of such tools.” — `docs/devel/code-provenance.rst:329`
[^fn5]: Limited to agentic tools whose generated code/content is included in a contribution to QEMU.
[^fn6]: contributor (patch author, via Signed-off-by)
[^fn7]: rationale, not a rule: “Where the training material is known, it is common for it to include large volumes of material under restrictive licensing/copyright terms.”

